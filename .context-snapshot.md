# 项目上下文快照 (Project Context Snapshot)

> 用途：在新会话开始时粘贴此提示词，让对方 5 分钟内接手整个项目
> 版本：2026-06-05 v2.2 终稿

---

## 一句话定位

`~/claude-workspace/SDLC/` 下的 **sdlc 项目**：AI 驱动的全流程 SDLC 编排 CLI 工具，类似 Claude Code + Cursor 但作为**独立 CLI 工具**卖给研发团队。

---

## 目录结构（当前状态）

```
~/claude-workspace/SDLC/
├── prd/                            # v2.2 产品 PRD（已完成）
│   ├── 00-README.md                # PRD 导航
│   ├── 01-core-concepts.md         # 7 抽象
│   ├── 02-stage-catalog.md         # 22 Stage + KB 映射
│   ├── 03-entry-points.md          # 12 入口
│   ├── 04-pipeline-builder.md      # DAG 算法
│   ├── 05-adapters.md              # 18 Adapter Schema
│   ├── 06-project-profiles.md      # 14 Profile
│   ├── 07-gate-catalog.md          # 10 Gate
│   ├── 08-state-and-meta.md        # meta.json + audit.log
│   ├── 09-stage-template.md        # Stage YAML 模板
│   ├── 10-running-examples.md      # 10 场景
│   ├── 11-subagent-and-skills.md   # Subagent + Skill
│   ├── 12-implementation-roadmap.md# 8 月 4 阶段路线
│   ├── 13-memory-and-evolution.md  # 3 层记忆 + L2 KB
│   ├── 14-init-and-bootstrap.md    # sdlc init
│   ├── 15-rule-and-standard-library.md  # v2.2 新增 547+ rules
│   └── v1-archive/                 # 11 旧 v1.0 + archive README（已归档）
│
├── doc/design/                     # 15 份实现设计稿（已完成）
│   ├── 00-README.md                # 设计稿索引 + 5 分钟上手
│   ├── 01-architecture-overview.md # 1 张架构图 + 6 子系统 + 3 时序 + 6 不变量
│   ├── 02-tech-stack.md            # 22 依赖 + 选型理由
│   ├── 03-module-design.md         # 13 包详细接口/类/职责
│   ├── 04-data-model.md            # SQLite 6 表 + 2 视图 + 文件 schema
│   ├── 05-cli-and-api.md           # 19 CLI 命令 + Python API + 退出码
│   ├── 06-stage-execution.md       # Stage 8 步生命周期 + 错误处理
│   ├── 07-extension-mechanism.md   # 4 层加载 + 零代码扩展
│   ├── 08-kb-engine.md             # Scanner 7 阶段 + Writer + 4 Enforcer
│   ├── 09-subagent-and-llm.md      # LLM 抽象 + Cache + Subagent Pool
│   ├── 10-state-and-recovery.md    # SQLite 状态机 + 12h Resume
│   ├── 11-testing-strategy.md      # 单元/集成/E2E/性能 + CI
│   ├── 12-distribution-and-release.md # uv + Homebrew + PyPI + Docker
│   ├── 13-dev-workflow.md          # sdlc 自身的 git/CI/版本/CHANGELOG
│   └── 14-milestones.md            # 8 月 4 阶段周级任务
│
├── .context-snapshot.md            # 本文件
└── （待建）sdlc.py + sdlc/         # P1 M1 实施
```

---

## 技术选型（已确定，不可改）

- **语言**：Python 3.11+（**用 click 8.1+**，不用 typer）
- **核心库**：click 8.1+ / rich 13.7+ / pydantic v2.6+ / ruamel.yaml 0.18+ / jinja2 3.1+ / httpx 0.27+ / anthropic 0.34+ / openai 1.30+ / pyyaml 不准用（ruamel 替代）
- **LLM**：Anthropic SDK 主 + OpenAI 回退（统一 `LLMClient` 抽象 + ModelRouter + SQLite Cache）
- **架构**：**独立 CLI 优先**（`sdlc.py` 入口），薄适配层兼容外部 agent
- **存储**：**SQLite + JSON/YAML 双轨**
  - SQLite：pipelines / stages / artifacts / llm_calls / kb_deltas / audit_log_meta（6 表）
  - JSON/YAML：meta.json / audit.log / KB / Profile / Adapter / Stage
- **包管理**：uv 0.4+（替代 pip + poetry） + pyproject.toml + uv.lock
- **构建**：hatchling（不选 setuptools / poetry-core）
- **质量**：ruff 0.4+（替代 flake8/black/isort）+ mypy 1.10+ strict + pre-commit 3.7+
- **测试**：pytest 8.2+ / pytest-asyncio / pytest-cov / respx / freezegun / hypothesis / syrupy / mutmut

**未选**：Go / TypeScript / Rust / Shell / Java（已否）；typer / FastAPI / Django / LangChain / AutoGen / CrewAI（已否）

---

## 关键设计要点

### 13 个包（`sdlc/`）

```
sdlc/
├── cli/          # 19 命令 (click)
├── utils/        # 纯函数 (pydantic / YAML / path / fs / lock / log)
├── core/         # 编排引擎 (Pipeline Builder / Stage Runner / Entry Detector / Gate Engine)
├── kb/           # 知识引擎 (Scanner / Writer / Reconciler / 4 Enforcer / ExceptionManager)
├── llm/          # LLM 抽象 (Provider / Cache / Router / Cost)
├── subagent/     # 智能体池 (Pool / Renderer / Tool)
├── adapter/      # 适配器框架 (Detector / Registry / dongboot / spring / python / ...)
├── stage/        # Stage 库 (Runner / Loader)
├── profile/      # 项目画像 (Loader / Matcher / 14 内置)
├── rule/         # 规则引擎 (Loader / Enforcer)
├── gate/         # 闸门引擎 (Loader / Engine)
├── audit/        # 审计 (Logger / Event)
├── state/        # 状态 (StateStore / Backup)
└── integrations/ # 外部集成 (MCP / Skill / Shell / HTTP)
```

**依赖方向强约束**（不可违反）：
- KB 不能 import Adapter
- Adapter 不能 import CLI
- Integrations 不能 import Engine

### 19 个 CLI 命令

```
run / init / status / resume / trace
rule {list/get/create/enable/disable/validate}
kb {scan/show/update/validate/reconcile}
stage / adapter / profile / agent / config / doctor / version
completion / replay / export / import / stats
```

### SQLite 6 表 + 2 视图

```sql
pipelines       (id, name, goal, status, created_at, updated_at, ...)
stages          (id, pipeline_id, stage_id, status, started_at, finished_at, ...)
artifacts       (id, pipeline_id, stage_id, type, name, path, content_hash, ...)
llm_calls       (id, pipeline_id, stage_id, model, input_tokens, output_tokens, cost_usd, duration_ms, cached, ...)
kb_deltas       (id, pipeline_id, stage_id, target, operation, fingerprint, created_at, ...)
audit_log_meta  (id, pipeline_id, level, type, ts, ...)  -- 详细在 audit.log JSONL
v_pipeline_summary
v_cost_daily
```

### Stage 8 步生命周期（详见 06-stage-execution.md）

1. 加载 + 准备（config / KB ref / dependencies）
2. 加载 KB（pre_kb_load）
3. 校验前置 artifacts
4. 注入规则
5. 调用 Subagent
6. 解析结果
7. 持久化（事务）
8. 写 KB（post_kb_update） → 触发 Gate

### 加载层级（4 层，高优先级覆盖低）

```
--config > 项目 .sdlc/ext/ > 用户 ~/.sdlc/ext/ > 内置 sdlc/builtin/
```

### 关键不变量（6 条，详见 01 §六）

1. **单一事实源**（meta.json 与 SQLite 对账）
2. **审计完整**（所有事件可追溯）
3. **KB 幂等**（相同内容不重复写）
4. **Stage 失败不留半成品**（事务回滚）
5. **Subagent 隔离**（不污染主上下文）
6. **配置可重现**（相同配置 → 相同结果）

---

## 重要约束

- **通用化**：适配任意需求、任意项目、任意技术栈，**支持任意阶段开始**
- v2.2 PRD 特性：3 层记忆（L1 Pipeline / L2 KB / L3 Subagent） + 自动 init + 上下文更新 + 结构化 rules/standards/architecture
- 设计稿**无任何可执行代码**（仅 YAML 模板/接口签名示意）
- DongBoot 是 **18+ Adapter 之一**，不是核心依赖
- v1.0 旧文档已全部归档到 `prd/v1-archive/`

---

## 当前状态

✅ **设计稿 15 份全部完成**（7616 行）
✅ PRD v2.2 16 份全部完成
❌ **还没有任何可执行代码**

**下一步**：进入 P1 M1 实施（按 `doc/design/14-milestones.md` 走）

```
M1 Week 1-2: 脚手架 + utils + 19 CLI stub
M1 Week 3-4: state + audit + LLM cache + dongboot adapter
M1 Week 5-6: subagent + stage + gate
M1 Week 7-8: 1 stage 端到端跑通（dongboot 真实项目）
```

**M1 目标**：v0.1.0 alpha，dongboot 项目能跑通至少 1 个 stage（如 `s-clarify`），单元测试覆盖 >= 60%

---

## 工程目录（待建）

```
~/claude-workspace/SDLC/
├── pyproject.toml           # uv 项目元数据
├── uv.lock
├── README.md
├── LICENSE
├── CHANGELOG.md
├── sdlc.py                  # CLI 入口
├── sdlc/                    # 主包（13 子包）
│   ├── __init__.py          # __version__ = "0.1.0"
│   ├── cli/
│   ├── utils/
│   ├── core/
│   ├── kb/
│   ├── llm/
│   ├── subagent/
│   ├── adapter/
│   ├── stage/
│   ├── profile/
│   ├── rule/
│   ├── gate/
│   ├── audit/
│   ├── state/
│   ├── integrations/
│   └── builtin/             # 内置 Subagent/Stage/Profile/Rule/Gate
│       ├── subagents/       # 11+ sa-*.yaml
│       ├── stages/          # 22 stage-*.yaml
│       ├── profiles/        # 14 profile-*.yaml
│       ├── rules/           # 547+ rule-*.yaml
│       └── gates/           # 10 gate-*.yaml
├── templates/               # Jinja2 模板
├── tests/                   # unit / integration / e2e / perf / contract / property
├── docs/                    # mkdocs 源
├── scripts/                 # 工具脚本
└── prd/ + doc/design/       # 已存在
```

---

## 运行时目录

```
<project_root>/.sdlc/
├── state.db                 # SQLite
├── audit.log                # JSONL
├── meta.json                # Pipeline 元数据
├── snapshots/               # 5 个最近
└── ext/                     # 项目级扩展（覆盖内置）

~/.sdlc/                     # 全局
├── config.toml              # 全局配置
├── agents/                  # 用户级 subagent
├── stages/                  # 用户级 stage
├── profiles/                # 用户级 profile
├── rules/                   # 用户级 rule
├── gates/                   # 用户级 gate
├── kb/                      # 全局 KB
└── projects/<id>/           # 全局项目状态（当 <project>/.sdlc 不存在时）
```

---

## 立即可做的下一步（按优先级）

1. **建工程骨架**（M1 W1-2）
   - `cd ~/claude-workspace/SDLC/`
   - `uv init` → 生成 `pyproject.toml` + 22 直接依赖
   - 写 `sdlc/__init__.py` + `sdlc/cli/main.py` + 19 命令 stub
   - 写 `sdlc/utils/`（pydantic / YAML / path / fs / lock / log）
   - 验证 `uv run sdlc --version` 输出 `sdlc 0.1.0`
   - 写 19 命令 `--help` 至少能跑
2. **完善 audit + state**（M1 W3-4）
3. **LLM cache + 1 个 Subagent 跑通**（M1 W5-6）
4. **1 个 stage 端到端 + dongboot 真实项目**（M1 W7-8）

---

## 关键文件链接（速查表）

- 架构：`doc/design/01-architecture-overview.md`
- 技术栈：`doc/design/02-tech-stack.md`
- 13 包接口：`doc/design/03-module-design.md`
- 数据模型：`doc/design/04-data-model.md`
- 19 CLI 命令：`doc/design/05-cli-and-api.md`
- Stage 生命周期：`doc/design/06-stage-execution.md`
- 扩展机制：`doc/design/07-extension-mechanism.md`
- KB 引擎：`doc/design/08-kb-engine.md`
- Subagent + LLM：`doc/design/09-subagent-and-llm.md`
- 状态恢复：`doc/design/10-state-and-recovery.md`
- 测试策略：`doc/design/11-testing-strategy.md`
- 分发发布：`doc/design/12-distribution-and-release.md`
- 开发流程：`doc/design/13-dev-workflow.md`
- 里程碑任务：`doc/design/14-milestones.md`

---

## 重要命令速查

```bash
# 进入项目
cd ~/claude-workspace/SDLC/

# 初始化 uv（待做）
uv init
uv add click rich pydantic ruamel.yaml jinja2 httpx anthropic openai
uv add --dev pytest pytest-asyncio pytest-cov pytest-mock ruff mypy pre-commit

# 跑测试（待做）
uv run pytest
uv run pytest --cov=sdlc --cov-report=html

# Lint
uv run ruff check .
uv run ruff format .
uv run mypy sdlc/

# CLI（待做）
uv run sdlc --version
uv run sdlc init
uv run sdlc run "做一个订单查询接口"
```

---

## 绝对不要

- ❌ 不读设计稿就动代码
- ❌ 自己造设计稿里没定义的接口
- ❌ 跳过用户直接输出大段代码
- ❌ 假设用户没读过某些文档就重新解释
- ❌ 用 typer / FastAPI / LangChain（设计稿已否）
- ❌ 引入未在 pyproject.toml 列出的依赖
- ❌ 写注释（除非用户明确要求）
- ❌ 单文件超过 500 行不拆

---

## 版本

- v2.2 终稿 (2026-06-05)
- 配套设计稿 15 份（doc/design/）
- 配套 PRD 16 份（prd/）
- 下一步：P1 M1 实施
