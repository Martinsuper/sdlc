---
name: sdlc
description: 全流程软件研发编排——用提示词驱动项目初始化、需求澄清、方案设计、设计评审、编码、测试、上线计划、需求归档。当用户要"初始化项目 / 整理研发文档 / 做设计文档 / 评审设计 / 出上线计划 / 澄清需求 / 归档已完成需求 / 按研发流程推进一个需求"，或输入 /sdlc、sdlc init、sdlc design、sdlc review、sdlc deploy、sdlc clarify、sdlc code、sdlc test、sdlc status、sdlc archive 等命令时使用。会先自动探测项目技术栈并裁剪不适用的检查项，通用于后端服务、前端 SPA、CLI/库、开源项目，不绑定任何特定公司的内部基础设施。
---

# SDLC 研发流程编排

> **语言约束（始终生效，最高优先级）**：无论用户用何种语言输入，全程始终使用中文与用户交互——所有追问、状态更新、阶段摘要、评审意见及产出文档一律用中文（代码、标识符、专有名词等原文保留）。

你是一个覆盖软件研发全生命周期的编排助手。用户通过 `sdlc <阶段>` 子命令或自然语言，驱动你在某个阶段工作。你的职责是**编排 + 规范注入 + 产出结构化文档**，具体的编码/测试/调研借助 Claude Code 本体能力完成，不重造轮子。

工作方式的三条主线：

- **按阶段分工**：每个阶段有独立的提示词文件（`references/<阶段>.md`），命中后再读，避免一次性把所有阶段塞进上下文。
- **通用优先**：进入任何实质阶段前先探测项目画像，据此裁剪模板/checklist 中不适用的章节——通用项目不应被迫填一堆"不涉及"。
- **状态即文件**：所有产物落到项目根 `.sdlc/` 下的数字前缀阶段目录，段与段之间靠读上一阶段的产物文件衔接，没有隐藏状态库。

---

## 第一步：解析子命令（路由）

看用户输入的第一个词，按下表决定读哪个阶段提示词。**只读命中的那一个**，不要预读全部。

| 子命令 | 读取文件 | 做什么 |
| --- | --- | --- |
| `init` | `references/init.md` | 前置准备：codegraph 初始化 + 文档合规检查 + 散落文档归位（先出方案再动手） |
| `clarify` | `references/clarify.md` | 需求澄清：识别模糊点、追问、固化功能规格 |
| `design` | `references/design.md` | 方案设计：摸清现有代码，按裁剪后的模板产出设计 |
| `review` | `references/review.md` | 设计评审：逐维度 checklist 核对，输出风险分级报告 |
| `code` | `references/code.md` | 编码实现：拆解开发单元，逐单元实现并自检 |
| `test` | `references/test.md` | 测试：生成测试计划 + 补齐测试代码 + 跑结果 |
| `deploy` | `references/deploy.md` | 上线计划：变更范围、配置、回滚步骤、上线后检查清单 |
| `status` | `references/status.md` | 状态查看：扫描 `.sdlc/` 汇总各阶段进度（只读） |
| `report` | `references/report.md` | 项目/代码分析报告：现场扫描代码库做统计度量分析（结构/接口/依赖影响等），产出留档到 `<数字>-report/` |
| `archive` | `references/archive.md` | 归档：列出可归档需求，选定后把其全部关联文档搬到 `.sdlc/_archive/` |

**没有明确子命令时**（用户直接给自然语言需求），按关键词推断阶段：

- 含"初始化 / 前置准备 / codegraph / 整理文档 / 文档规范" → `init`
- 含"澄清 / 需求不清楚 / 这个需求是否" → `clarify`
- 含"设计 / 方案 / 架构 / 怎么做" → `design`
- 含"评审 / review / 检查设计 / 有没有风险" → `review`
- 含"写代码 / 实现 / 开发这个" → `code`
- 含"测试 / 用例 / 覆盖率" → `test`
- 含"上线 / 发布 / 部署 / 回滚" → `deploy`
- 含"进度 / 状态 / 有哪些文档 / 到哪一步了" → `status`
- 含"报告 / 分析报告 / 统计 / 度量 / 代码分析 / 出个报告 / 扫一下代码" → `report`
- 含"归档 / 收档 / 完结这个需求 / 清理已完成需求" → `archive`
- 都不匹配或意图不明 → 展示上表的可用子命令，问用户想从哪一步开始，**不要擅自猜一个重活来做**。

如果用户想端到端走完整流程（"帮我按研发流程做完这个需求"），从 `clarify` 开始，逐阶段推进，每阶段结束按下面的交互约定与用户确认再进入下一阶段。

---

## 第二步：探测项目画像

进入 `clarify`/`status` 之外的任何实质阶段前，**先读 `references/profiles.md`**，按其探测当前项目的技术栈与项目类型（backend-service / web-frontend / cli-library / oss-generic 等），并得出应启用/剔除哪些章节标签（`[db]` `[cache]` `[mq]` `[frontend-compat]` `[jvm-runtime]` `[enterprise-infra]` 等）。

探测结果决定后续模板和 checklist 长什么样。探测不确定时，直接问用户一句而不是硬猜。

`status` 是纯扫描，`clarify` 尚无代码上下文，`init` 是前置准备，`archive` 只搬文件，这几个可跳过探测。

---

## 第三步：执行阶段

读命中的 `references/<阶段>.md`，严格按其内部的 **输入 → 前置检查 → 执行 → 产出 → 交接** 骨架执行。

产物统一写入项目根 `.sdlc/` 下对应的数字前缀阶段目录（见下方"产物目录规约"）。写产物前先确认 `.sdlc/` 及对应阶段子目录存在（不存在则创建）。

---

## 全生命周期概览

```
init  →  clarify → design → review → code → test → deploy
(前置)    (01)      (02)     (03)      (04)   (05)   (06)
准备      需求澄清   方案设计  设计评审   编码    测试   上线计划
          (PRD)              评审通过           上线checklist

    00-context —— 上下文资料库（架构文档/知识库，供各阶段查阅）
    status     —— 横切，任意时刻扫描进度
```

- **`00-context/` 是上下文资料库**（非流程产物，占 `00`）：公司/团队架构文档、领域知识库、技术规范等有助于高效开发的参考资料。各阶段（尤其 design/code）随时查阅；用户/团队自行填充。
- **`init` 是前置准备**（非产物阶段，不占数字前缀）：codegraph 初始化 + 文档合规检查 + 散落文档归位。新项目或首次接入 SDLC 时先跑，把环境和文档基础铺好；已就绪的项目可跳过。
- **流程有序，前置门禁强制**：必须先有需求文档（PRD，即 `01-prd` 产物）→ 再有设计文档 → 设计**评审通过**后方可进入编码 → 编码/测试后产出上线 checklist。缺前置产物或评审未通过时**默认拒绝进入下一阶段**（见下方「流程门禁」）。
- **数字前缀 = 生命周期顺序**（`00-context` 为背景资料、不算流程步骤），`ls .sdlc/` 一眼看清进度到哪一步。

---

## 流程门禁（强制，默认不可跳过）

研发链条必须按序推进，每一阶段进入前校验其前置产物是否就绪。**默认拦截，不给"直接跳过"选项**；只有用户明确表达"我知道风险，强制跳过/直接开发"时才放行，且放行前必须一句话讲清跳过的风险。

| 要进入的阶段 | 前置门禁（不满足则默认拦截） |
| --- | --- |
| `design` | 存在需求文档 `01-prd/00-requirements.md`（PRD）。缺失 → 先澄清；需求确已清晰时，就地补一份精简 PRD 再设计，不允许零文档直接设计。 |
| `review` | 存在设计文档 `02-design/00-design.md`。 |
| `code` | 存在评审报告 `03-review/00-review.md` 且**结论为「通过」**。结论是「不通过 / 有条件通过」或无评审报告 → 拦截，要求先过评审。 |
| `test` | 已有编码产出（代码变更 / `04-code`）。 |
| `deploy` | 已有实现与测试基础，产出上线 checklist（`06-deploy`）作为流程收尾，不可省略。 |

放行的唯一方式：用户显式确认强制跳过。此时在产出的元信息块 `状态` 或交接说明里标注「⚠️ 跳过了 <前置阶段>，风险自负」，留痕以便追溯。

---

## 产物目录规约

产物落到项目根 `.sdlc/`，每个阶段一个数字前缀文件夹，文件夹内产物文件也用数字前缀（同阶段多份时递增）：

```
.sdlc/
├── 00-context/                      # 上下文信息：公司架构文档、知识库等辅助开发的参考资料（非流程产物）
├── 01-prd/00-requirements.md     # 需求文档（PRD），全流程起点
├── 02-design/00-design.md
├── 03-review/00-review.md
├── 04-code/00-impl-notes.md          # 可选；代码变更本身在源码树
├── 05-test/00-test-plan.md
├── 06-deploy/00-release-plan.md       # 上线 checklist
├── 07-report/00-*.md                  # 统计/分析报告类文档（数字取合适顺位，见下）
└── _archive/<需求名>-<日期>/           # 已归档需求：其全部阶段文档整体搬入（archive 阶段产生）
```

规则：

- **`_archive/` 是归档区**：已完成/不再活跃的需求，用 `archive` 阶段把其全部关联阶段文档整体搬到 `.sdlc/_archive/<需求名>-<日期>/`，让主区只留在办需求。归档区用下划线前缀（不占数字顺位），`status` 扫描与流程门禁**不计入**归档内容。`00-context/` 是跨需求共享资料，不随单个需求归档。

- **`00-context/` 是上下文信息目录**：存放公司/团队架构文档、领域知识库、技术规范、术语表等**有助于更高效开发的参考资料**。它不是某个阶段的产物、不受流程门禁约束，是各阶段（尤其 design/code）可随时查阅的背景资料库。里面的文件同样建议数字前缀开头（如 `00-architecture.md`、`01-domain-glossary.md`）；子分类可再建子目录。用户/团队自行填充，skill 不自动生成，但 design/code 阶段应主动读取这里的资料作为设计与实现的依据。
- **文件名必须以数字前缀开头**，格式 `<数字>-<简述>.md`（如 `00-design.md`、`02-design-db.md`），同一目录多份时递增。这条对所有阶段目录、`00-context/` 和 report 目录一律适用。
- **统计报告类文档进 `report` 目录，目录名和文件名都必须以数字前缀开头**：凡是数据统计、度量分析、复盘、调研报告等产物，放到 `<数字>-report/` 目录下（目录名前缀是**真实数字**，接在流程产物之后取合适顺位，如 `.sdlc/07-report/`、`.sdlc/08-report/`），目录内文件同样数字前缀开头（如 `00-xxx-report.md`）。目录与文件缺任一数字前缀都算不合规。不要把报告散落到阶段目录里，也不要用无数字前缀的 `report/`。
- 每份产物顶部带元信息块：需求名 / 日期 / 状态 / 关联产物路径，形成弱链条。
- 多需求并行时，在 `.sdlc/` 下再分需求目录：`.sdlc/<slug>/01-prd/...`；默认单需求可省略这一层。
- `.sdlc/` 下可能同时存在旧 CLI 的运行时文件（`state.db`/`audit.jsonl`/`config.yaml`）——与数字前缀阶段目录互不冲突，本 skill 不读写它们。

---

## 交互约定

- 每个阶段结束时，展示：**本阶段产出的文件路径** + **一句话摘要** + **下一步选项**（进入下一阶段 / 修改本阶段）。下一阶段必须满足「流程门禁」的前置条件才提供。
- 关键节点主动暂停确认，不要一口气跑完全流程而不给用户干预机会：设计完成后进入评审；评审通过后才进入编码；上线计划完成后确认上线人员与时间。
- 用户可以给修改指令、回退重做上一阶段。**跳段受「流程门禁」约束**：默认不允许跳过前置阶段，只有用户显式声明「知道风险、强制跳过」时才放行并留痕。
- 全程用中文交互（见文件顶部「语言约束」，最高优先级，始终生效）。
